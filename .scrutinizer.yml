checks:
    javascript: true
build:
    environment:
        variables:
            JWT_SECRET: 'scrutsecret'
    nodes:
        analysis:
            tests:
                override:
                    - js-scrutinizer-run
        tests: true
