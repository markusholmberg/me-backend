build:
    environment:
        variables:
            JWT_SECRET: "scrutinizersecret"
    nodes:
        coverage:
            tests:
                override:
                    - command: npm test
                    coverage:
                        file: coverage/clover.xml
                        format: clover
